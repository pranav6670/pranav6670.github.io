# macOS
.DS_Store

# Playwright MCP / browser-automation session artifacts
.playwright-mcp/

# Verification screenshots (root-level only; real site images live under images/)
/*.png
/*.jpeg

# Local working docs (design specs / implementation plans) — not published
docs/superpowers/
